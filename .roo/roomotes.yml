version: "1.0"

commands:
  - name: Install bun
    run: curl -fsSL https://bun.sh/install | bash
    timeout: 30
  - name: Install dependencies
    run: ~/.bun/bin/bun install
    timeout: 30
  - name: Run dev
    run: nohup ~/.bun/bin/bun run dev > dev.log 2>&1 &
    timeout: 30
