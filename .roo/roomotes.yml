version: "1.0"

commands:
  - name: Install bun
    run: curl -fsSL https://bun.sh/install | bash
    timeout: 60
  - name: Install dependencies
    run: bun install
    timeout: 60
  - name: Run dev
    run: nohup bun run dev > dev.log 2>&1 &
    timeout: 60
