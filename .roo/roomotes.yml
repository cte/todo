version: "1.0"

services:
  - redis7
  - postgres16

commands:
  - name: Install bun
    run: mise use bun
    timeout: 60
  - name: Install dependencies
    run: bun install
    timeout: 60
  - name: Run dev
    run: nohup bun dev > dev.log 2>&1 &
    timeout: 10
